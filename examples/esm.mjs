/**
 * Native ES Module usage example for @sebastienrousseau/size-limit-config
 */
import config from "../index.mjs";

console.log("=== ESM Usage Example: @sebastienrousseau/size-limit-config ===");
console.log("Loaded via ESM default export:", typeof config);
