site_name = "size-limit-config"
site_title = "@sebastienrousseau/size-limit-config — Shareable Size Limit configuration rules enforcing strict performance bundle budgets."
site_description = "Shareable Size Limit configuration rules enforcing strict performance bundle budgets."
base_url = "https://sebastienrousseau.com/size-limit-config/"
language = "en-GB"

content_dir = "web/content"
template_dir = "web/_layouts"
output_dir = "public"

[agents]
agents_txt = true
ai_plugin = true

[agents.mcp]
enabled = true
