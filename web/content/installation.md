---
title: "Installation — @sebastienrousseau/size-limit-config"
description: "How to install and set up @sebastienrousseau/size-limit-config."
layout: "doc"
---

# Installation

Install `@sebastienrousseau/size-limit-config` as a development dependency using your preferred package manager.

## npm

```bash
npm install --save-dev @sebastienrousseau/size-limit-config
```

## pnpm

```bash
pnpm add -D @sebastienrousseau/size-limit-config
```

## yarn

```bash
yarn add -D @sebastienrousseau/size-limit-config
```

## bun

```bash
bun add -d @sebastienrousseau/size-limit-config
```

## From Source

```bash
git clone https://github.com/sebastienrousseau/size-limit-config.git
cd size-limit-config
make test
```
