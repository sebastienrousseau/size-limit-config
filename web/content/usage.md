---
title: "Usage — @sebastienrousseau/size-limit-config"
description: "How to use and configure @sebastienrousseau/size-limit-config."
layout: "doc"
---

# Usage

`@sebastienrousseau/size-limit-config` can be consumed across all standard module formats.

## CommonJS

```javascript
module.exports = require("@sebastienrousseau/size-limit-config");
```

## ES Modules

```javascript
import config from "@sebastienrousseau/size-limit-config";
export default config;
```

## In `package.json`

```json
{
  "size-limit": "@sebastienrousseau/size-limit-config"
}
```
