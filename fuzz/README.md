# Fuzz Testing for `@sebastienrousseau/size-limit-config`

The fuzz testing suite validates configuration integrity against random, boundary, and malformed inputs.

## Running Fuzz Tests

```bash
node fuzz/fuzz_config.js
```
